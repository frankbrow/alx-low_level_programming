#include "main.h"
#include <unistd.h>
/**
 * _putchar - write the character c stdout
 *@c: The character to print
 *
 * Return: on success 1.
 * on error, -1 is retured, and error is set approximatively.
 */
int _putchar(char c)
(
        return (write(1, &c, 1));
) 
